'use client';

import { ReactNode } from "react";

export default function CarrierTripsLayout({ children }: { children: ReactNode }) {
    return (
        <>
            {children}
        </>
    )
}
