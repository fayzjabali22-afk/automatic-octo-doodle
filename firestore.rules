rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }
    
    match /users/{userId} {
      allow read, write: if isOwner(userId);
    }

    match /trips/{tripId} {
      // Allow any user (signed-in or anonymous) to read/list trips
      allow read: if true;
      // Allow create if the user is the owner
      allow create: if isOwner(request.resource.data.userId);
      // Allow update/delete if the user was the original owner
      allow update, delete: if isOwner(resource.data.userId);
       // Allow queries by carrierName and carrierPhoneNumber
      allow list: if true;
    }

    match /bookings/{bookingId} {
      allow read, create: if isSignedIn();
    }
    
    match /users/{userId}/notifications/{notificationId} {
      allow read, list, write: if isOwner(userId);
    }
  }
}
