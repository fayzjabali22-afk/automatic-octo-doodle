rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Development mode: allow all reads and writes to all collections.
    // Using {document=**} as the most direct way to match all documents.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
